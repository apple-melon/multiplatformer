# Render Blueprint: https://render.com/docs/blueprint-spec
services:
  - type: web
    name: multiplatformer
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /
